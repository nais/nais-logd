fluentd:
  name: fluentd
  image:
    repository: navikt/nais-logd
    tag: 61
    pullPolicy: IfNotPresent
  updateStrategy: RollingUpdate
elasticsearch:
  hosts: elasticsearch-logging:9200
  index_prefix: logstash-apps-test
cluster:
  name: kubernetes
  envclass: t
kubewatch:
  enable: true
  name: kubewatch
  image:
    repository: softonic/kubewatch
    tag: v0.3.3
    pullPolicy: IfNotPresent
volumes:
  - name: varlog
    mountPath: /var/log
    hostPath: /var/log
    readOnly: false
  - name: varlibdockercontainers
    mountPath: /var/lib/docker/containers
    hostPath: /var/lib/docker/containers
    readOnly: true
# Add to make logs available in minikube
#   - name: sda1
#      mountPath: /mnt/sda1
#      hostPath: /mnt/sda1
#      readOnly: true
    
# Add to override conf file in docker image
# configFiles:
#   fluent.conf: |-
#     <match **>
#       @type ...
#     </match>
